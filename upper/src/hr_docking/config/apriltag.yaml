# Tag family is frozen by the technical plan; size and IDs await physical freeze.
apriltag:
  ros__parameters:
    family: tag36h11
    # Printed edge length in metres. MUST be measured on the actual plate.
    size: 0.0
    max_hamming: 0
    detector:
      threads: 2
      decimate: 2.0
      blur: 0.0
      refine: true
