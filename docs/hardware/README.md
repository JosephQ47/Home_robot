# 硬件说明（技术方案 §9.1 要求的 docs/hardware/）

按技术方案，本目录存放：**接线、CAN ID、轮序、坐标系和急停**。

当前为空，因为这些参数尚未实测冻结——方案 §10.4 明确要求未实测的参数保持 TBD，
不许用猜测值占位。已知的硬件事实暂时散在 STATUS.md 的时间线里，等实测后
再按主题迁移到这里。

已有但待整理进本目录的内容：

| 主题 | 现在在哪 | 状态 |
|---|---|---|
| USB 串口稳定命名（三个设备的 VID/PID/序列号/接口号） | `config/udev/` 与 STATUS.md | 已实测，规则文件已写，待 sudo 安装 |
| 下位机串口参数（230400 8N1、旧 X-Protocol 帧格式） | STATUS.md、`docs/protocol/` | 已实测 |
| 毫米波接线（3.3 V TTL、TX/RX 交叉）与量程标定 | `upper/src/hr_hmmd/README.md` | 已实测；标定不可复现，见该 README |
| CAN ID、轮序、坐标系、急停 | — | **未实测，保持 TBD** |
