# 家庭服务机器人项目任务清单

更新时间：2026-09-18（按新版技术方案更新）  
权威架构：[家庭服务机器人技术方案](../家庭服务机器人技术方案.md)  
当前载体：PC/Ubuntu 虚拟机临时代替 RK3588；正式方案仍使用 RK3588。

## 状态规则

| 状态 | 含义 |
|---|---|
| 已实测 | 已在当前硬件或真实数据上保存验证结果 |
| 框架完成 | 接口、包、配置或 Mock 可构建运行，尚未完成实机验收 |
| 待实施 | 前置条件具备但尚未开发 |
| 阻塞 | 缺少硬件、参数、协议或依赖，禁止用猜测值继续 |

## 当前安全边界

- Mock 模式只运行 ROS 节点，不打开 STM32、HMMD、相机或雷达设备，不发布速度。
- `robot_bringup.launch.py` 的 STM32 串口、命令输出和 HMMD 串口默认均为关闭。
- 不启动底盘运动，不烧录 Hi3516/STM32，不修改启动分区。
- 未实测的外参、底盘参数、停止区、串口协议和量纲统一保持 `TBD`。

## 阶段任务

| ID | 状态 | 任务与产物 | 前置依赖 | 验收或证据 | 阻塞原因/下一步 |
|---|---|---|---|---|---|
| F0-01 | 框架完成 | ROS 工作区、安全默认值和五种 bringup 入口 | ROS 2 Humble | 全量 `colcon build` | 真实模式仍需补系统依赖 |
| F0-02 | 框架完成 | Mock 模式禁止硬件和速度输出 | F0-01 | ROS graph 无 `/cmd_vel` 发布者 | 后续每次集成都必须回归 |
| F0-03 | 框架完成 | USB 串口稳定命名规则：`robot_mcu`、`hmmd_sensor`、`hi3516_debug` | 当前实测 VID/PID/序列号/接口号 | 脚本语法及 udev 属性核对通过 | 安装到 `/etc/udev/rules.d` 需要用户执行一次 sudo |
| F1-01 | 框架完成 | `hr_interfaces` 第一版消息与 `ExecuteTask` Action | 技术方案接口章节 | `ros2 interface show` | 接口变更必须同步协议评审 |
| F1-02 | 框架完成 | `hr_description` 仅发布 `base_link` 骨架 | 无 | URDF 可加载 | 相机、雷达、IMU 外参未测，未发布伪 TF |
| F1-03 | 框架完成 | `hr_simulation` 状态、里程计、IMU、扫描、TF 和 Nav2 Action 替身 | F1-01 | Mock bringup topic/action 检查 | 仅用于软件测试，不代表硬件性能 |
| F2-01 | 已实测 | Hi3516 RTSP → ROS Image → CPU YOLO → `/detections` | 板端 RTSP、WiFi | 09-07 实拍链 PASS；抓图 `captures/hi3516_rtsp_recheck_0906.jpg` | 更正：此前记的「WiFi 数据面异常」实为路由器过滤 ICMP，ARP 与 TCP 均正常。码流实测只有 `use_mode=0`(4M30) 可用 |
| F2-02 | 已实测 | 交叉编译最小程序并在 Hi3516 执行 | SDK/工具链 | `HI3516_APP_CHECK_OK` | 无 |
| F2-03 | 已实测 | `hr_camera` 支持 synthetic/RTSP/V4L2 启动参数 | 无 | RTSP 实拍端到端 PASS（2560x1440）；synthetic 启动 | UVC Device/UDC 尚未打通，RTSP 为临时替代；图像率随板端与虚拟机状态在 1~6 Hz 漂移 |
| F2-04 | 框架完成 | `hr_perception` 承载 CPU YOLO 识别与 stub | 本地模型 | stub 不伪造检测；CPU 后端沿用既有测试 | RK3588 RKNN 后端待实现 |
| F3-01 | 已实测 | `hr_bridge` 改用 `/wheel/odom_raw`、`/imu/data`、`/robot_status`，并以纯接收模式接入当前控制器 | F1-01 | 230400 8N1，`0x10` 状态约 50 Hz；未发送数据 | 旧协议缺 CRC/seq/stamp/安全状态 |
| F3-02 | 框架完成 | HomeRobot V1 协议验证草案、CRC16、序号、时间戳、`CMD_MOTION` 与共享测试向量 | STM32 固件与硬件评审 | 下位机 C 与 `hr_bridge` Python 对同一向量测试通过 | 草案不修改技术方案；快慢状态载荷、错误码和版本兼容仍需评审后冻结 |
| F3-03 | 已实测 | 伪串口状态接收、命令发送、限幅和命令超时归零集成测试 | F3-02 | `verify_bridge_pty` 与 `verify_full_control_pty` 均 PASS，未接触真实串口 | V1 半帧/粘包/乱序和真实断线重连仍等待协议冻结 |
| F4-01 | 框架完成 | EKF、SLAM、AMCL 配置目录和互斥入口 | 轮速、IMU、二维扫描、TF | Mock 拓扑 | `robot_localization` 尚未安装 |
| F4-02 | 阻塞 | 真实二维环境扫描与 `/scan` | 合适的平面扫描传感器 | 静态/动态障碍扫描记录 | HMMD 不提供角度扫描，不能承担此接口 |
| F4-03 | 待实施 | 若仍需自主导航，选择兼容的二维环境传感器并完成驱动、udev、标定 | F4-02 | 频率、范围、时间戳和掉线恢复 | 当前只确定 HMMD，无法提供该数据 |
| F5-01 | 框架完成 | Nav2 与 Collision Monitor 配置骨架 | F4 | 配置安装、真实启动显式使能 | footprint、速度、停止区均为 TBD |
| F5-02 | 阻塞 | 冻结 `/cmd_vel_auto → Collision Monitor → /cmd_vel` 实车链 | 二维扫描、制动测试、STM32 | 无旁路发布者、障碍停车 | 禁止在缺少扫描和制动数据时运动 |
| F5-03 | 框架完成 | 无地图 `odom` 短程联调替身 `/nav2_local_controller_adapter`（包 `hr_local_motion`） | 新 STM32 状态、里程计、Collision Monitor | 纯控制测试；默认启动无速度发布者 | 对应正式 Nav2；实车启用仍被 F3-02、F5-02 阻塞 |
| F5-04 | 已实测 | 安全 Mock 速度链 `/goal_pose → /cmd_vel_auto → collision_monitor → /cmd_vel → hr_bridge` | F5-03 | 0.30 m 目标、0.10 m/s 限速、0.20 m 障碍停车、唯一速度发布者均通过；`verify_full_control_pty` 已打通到 UART 字节 | 桥接传输和命令输出均关闭；不能代替实车制动验收 |
| F6-01 | 框架完成 | `hr_target_tracker` 失效安全骨架及临时 `person_hmmd` 模式 | 检测、相机内参/HFOV、HMMD | 默认关闭；数据缺失或多人时 `follow_allowed=false` | 正式 CameraInfo/二维扫描关联数学暂未启用 |
| F6-02 | 阻塞 | bbox 方位、二维雷达角窗/聚类、目标锁定 | 相机内参和相机-雷达外参 | rosbag 误关联/丢失测试 | HMMD 无目标方位，不能完成角度关联 |
| F6-03 | 待实施 | 推荐跟随位姿与 Nav2 动态目标限频 | F6-02、F5 | 丢失/跳变/过近时取消 | 等待实测参数 |
| F6-04 | 框架完成 | 已验证 `/follow_target` 的限速视觉伺服控制入口 | 目标方向、可靠距离、F5-02 | 越界、NaN、过近均输出零线速度 | HMMD 仅能给人体径向距离；其他物体缺少距离 |
| F6-05 | 已实测 | HMMD 目标距离标定，`range_scale_m` 落配置 | 实物雷达、卷尺 | 三点标定 1/2/3 m → `range_raw` 66/112/150，`range_scale_m=0.018782`，样本存 `hmmd_calibration_samples.json` | **标度不可复现**（同日两次 1 m 处得 50 与 66）；官方称不建议精准测距。只可作远/近粗判据，`minimum_safe_distance_m` 必须留余量。雷达接线接触不良需重插 |
| F7-01 | 已实测 | `hr_task_manager` Action、原子互斥、准入、取消、超时、接管中断 | F1、Mock Nav2 | 六类 Action 生命周期自动验收全部通过 | 真实地点/路线配置为空；正式入口保持拒绝 |
| F7-02 | 框架完成 | `hr_web_ui` ROS 状态适配与 HTTP 后端 `--adapter ros` | F1、F7-01 | Action readiness、提交/反馈/结果映射 | 默认仍为安全 Mock；持久化尚未实现 |
| F7-03 | 待实施 | 网页状态完整回写和任务审计持久化 | F7-02 | 网页刷新/断线/进程重启测试 | 需冻结持久化格式 |
| F7-04 | 待实施 | scheduler、地图/区域/路线校验与日志归档 | 已验收地图 | 重复触发、冲突和错过策略测试 | 地图尚不存在 |
| F8-00 | 框架完成 | `lower/` 技术方案验证骨架与可移植安全核心，命名采用 monitor/chassis/imu/command 四任务 | 技术方案、F3-02 | GCC `-Werror`、CTest、ASan/UBSan 均通过 | 用于快速验证，不替代正式工程；当前不生成可刷写固件 |
| F8-01 | 阻塞 | C 板 FreeRTOS 静态任务、BSP 与 IPC 硬件接入 | C 板工程、引脚/BOM、工具链 | WCET、超时、看门狗测试 | OpenCTR vendor 源码的 MCU、IMU、电机接口和任务结构均不同于正式 C 板方案 |
| F8-02 | 阻塞 | HT-10A SBUS、电机、CAN、急停和接管验收 | 硬件参数 | 实车安全测试 | 禁止当前阶段运动 |
| F8-03 | 框架完成 | OpenCTR 原厂工程安全验证副本：200 ms 命令超时归零及串口帧边界检查 | 厂商 V3.61 源码 | `test_ax_failsafe.c` 宿主机测试通过 | 缺 Keil/Arm Compiler，未完成整工程编译或刷写，当前板仍是旧固件 |
| F9-01 | 阻塞 | Hi3516 UVC Gadget 与 USB Device 枚举 | 可用 UDC/Device 端口或固件支持 | 20 次拔插、24h 连续流 | 当前 Type-C 实测为 Host，UDC 为空 |
| F9-02 | 待实施 | RK3588 部署、systemd、日志轮转和资源评估 | RK3588 到位 | 冷启动、长稳、故障恢复 | 当前 PC 只作替代调试 |

## HMMD 当前替代硬件记录

用户指定当前调试改用微雪 `HMMD-mmWave-Sensor`，正式技术方案原文不修改。官方资料说明该模块为 24 GHz 人体微动/存在传感器，UART 115200、约 100 ms 刷新，提供有人/无人、粗距离和 16 个距离门能量。

框架中的 `hr_hmmd` 发布 `/hmmd/detection`，不会发布 `/scan`。它可用于人体存在确认和粗距离实验，不能提供目标角度、二维地图、通用障碍轮廓或 Collision Monitor 所需平面扫描。因此：

- HMMD 可以成为视觉“检测到人”的辅助确认输入。
- HMMD 距离量纲必须用真实串口帧核对后才能使用。
- SLAM、AMCL、Nav2 障碍物地图、Collision Monitor 和视觉-雷达角度关联仍保持阻塞。

2026-09-06 串口实测：HMMD 接在 CH344 C 路，对应 Ubuntu `/dev/ttyACM2`
（USB interface `04`）；CH344 D 路 `/dev/ttyACM3` 仍为 Hi3516 调试串口。以
115200 读取到固件版本 `v1.6.1`，正常/调试/上报模式命令均返回成功 ACK。当前尚未
收到周期测量帧，需重新上电并在 1 m、2 m、3 m 处进行人体触发和距离量纲标定。
ROS 节点已实测能打开 ACM2，诊断为 `serial open; no report frame received`；底盘与
速度输出保持关闭。

2026-09-06 无地图控制 Mock 实测：向 `/goal_pose` 发布 `odom` 下 0.3 m 目标，
`hr_local_motion` 产生 29 条非零控制，最大线速度 0.10 m/s，随后归零。持续发布
模拟单人目标（2.0 m、0.2 rad）时产生 68 条跟随控制，最大线速度 0.10 m/s、最大
角速度 0.24 rad/s；目标停止上报后自动归零。测试接收端不转发 `/cmd_vel`，未连接
真实底盘。

2026-09-06 重新插拔后枚举：原 CH344 四路 `1a86:55d5` 已从虚拟机断开，因此此前
HMMD `/dev/ttyACM2`、Hi3516 `/dev/ttyACM3` 的映射当前失效。新接入的下位机 Type-C
枚举为 `1a86:55d4 USB Single Serial`，序列号 `5B34018995`，当前设备名
`/dev/ttyACM0`。设备权限为 `root:dialout 0660`，用户 `luckfox` 尚未加入 `dialout`，
所以协议只读采样被权限阻塞；尚未向该端口发送数据。

权限修复并重新枚举后，下位机固定标识为
`/dev/serial/by-id/usb-1a86_USB_Single_Serial_5B34018995-if00`，本次设备名
`/dev/ttyACM0`。230400 8N1 下连续10秒收到 501 个校验正确的
`AA 55 19 10` 状态帧，约 50.1 Hz，与现有旧 X-Protocol 解析器匹配。ROS纯接收
实测 `/wheel/odom_raw` 约 50.4 Hz、速度 `(0,0,0)`、电池约 11.46 V；
`/cmd_vel` 发布者为0，未向下位机发送任何数据。旧帧未包含正式方案要求的安全许可、
看门狗、控制源、序号和时间戳，故 `safety_permit=false`，不能据此启动实车运动。

2026-09-06 收到并审查 `OpenCTR_H60V36_R750ROS_V3.61.0602.zip`。源码确认 USB ROS
串口为 230400、上行 `0x10`、速度命令 `0x50`、50 Hz，与实机上行数据一致。源码同时
确认运动默认使能，且没有找到上位机命令超时清零或实际 IWDG 门控；一次非零命令可能
在通信断开后继续生效。因此继续保持 `command_output_enabled=false`，在完成固件断链停车
改造与台架故障注入前禁止实车 ROS 速度输出。完整证据见
[OpenCTR V3.61 下位机源码审查](OpenCTR_V3.61源码审查.md)。

随后按技术方案新增 `lower/` 下位机验证工程骨架。该代码用于快速验证技术方案，不修改
正式方案，也不视为已完成的量产固件。已实现 CRC-16/CCITT-FALSE、流式
帧解析、序号和时间戳校验、V1 `CMD_MOTION`、150 ms 上位机命令超时、100 ms 遥控
命令超时、遥控接管回中门、安全/任务心跳门、上电心跳准入、故障锁存和显式清除、
差速逆解和速度斜坡。安全许可撤销时立即清除内部速度状态。主机 GCC 严格告警构建、
CTest 和 ASan/UBSan 测试均通过。
协议权威说明位于 [docs/protocol/README.md](protocol/README.md)。板级参数仍为 `TBD`，
所以没有生成或刷写 STM32 固件。

`hr_bridge/home_protocol.py` 已实现同一 V1 帧的 Python 编码和流式解析，共享命令向量
跨 C/Python 校验通过。ROS 节点当前仍使用旧 `0x10` 纯接收路径；在正式 `STATE_FAST`
和 `STATE_SLOW` 载荷冻结并由下位机实现前，不把 V1 发送路径接到真实串口。

资料来源：[微雪 HMMD 官方 Wiki](https://www.waveshare.net/wiki/HMMD-mmWave-Sensor)。

## 常用验收命令

```bash
cd /home/luckfox/d2lros2
source Home_robot/upper/vision_env.sh
cd Home_robot/upper/src
python -m colcon build --symlink-install
source install/local_setup.bash
export ROS_DOMAIN_ID=61 ROS_LOCALHOST_ONLY=1
ros2 launch hr_bringup mock.launch.py
```

另开同环境终端检查：

```bash
ros2 node list
ros2 topic list
ros2 action list
ros2 topic info /cmd_vel --verbose
ros2 topic echo /follow_target --once
```

预期 Mock 模式存在 `/task/execute`、`/robot_status`、`/wheel/odom_raw`、`/imu/data`、`/scan`、`/camera/image_raw`、`/detections`、`/follow_target` 和 `/diagnostics`；`/cmd_vel` 不得有发布者。

## 本轮框架实测结果

- 全量构建：15 个 ROS 2 包完成。
- 单元回归：视觉公共逻辑、HMMD 分帧解析、任务状态机共 6 项通过；网页后端 3 项通过。
- Mock Action：正常任务完成；安全许可关闭时 PRECHECK 拒绝；并发任务被拒绝；客户端取消返回 `CANCELED`；遥控接管返回 `INTERRUPTED`。
- Web-ROS：HTTP `POST /api/tasks` 经 `--adapter ros` 转入 `/task/execute`，结果回写为 `DONE`。
- 安全拓扑：Mock 模式中 `/cmd_vel`、`/cmd_vel_auto` 均不存在；目标跟踪在缺少标定时发布 `follow_allowed=false`。
- 默认实机入口：日志确认 `hr_bridge transport disabled (safe mode)`；HMMD 串口与命令输出未启用。
- 自动化验收：`python Home_robot/upper/tools/verify_mock_framework.py` 输出 `PASS`。
- 控制链验收：`python Home_robot/upper/tools/verify_motion_chain.py` 通过短程目标、跟随、目标丢失停车、障碍停车及唯一速度路径检查；硬件输出保持关闭。
- 2026-09-06 回归：15 个 ROS 包构建通过，标准 `colcon test` 执行 12 项且全部通过；连同仓库级协议/网页测试共 21 项 Python 测试通过，`lower/` CTest 与 OpenCTR 超时保护宿主机测试通过。
- 任务生命周期：`python Home_robot/upper/tools/verify_task_lifecycle.py` 已验证完成、忙碌拒绝、安全准入拒绝、取消、超时和遥控中断；未配置跟随安全距离时保持拒绝。
- 串口桥接：`python Home_robot/upper/tools/verify_bridge_pty.py` 已验证旧状态帧进入 ROS、速度限幅、串口输出和 0.25 s 超时归零；测试仅使用伪终端。

---

## 2026-09-18 · 按新版技术方案更新

技术方案本轮变更详见 [ROS节点与技术方案对应表](ROS节点与技术方案对应表.md#与旧版本的差异本次按新技术方案更新)。
核心是：图像源由 Hi3516/SC4336P UVC 改为 **Orbbec Gemini 2 深度相机**，并新增
**电池/BMS + AprilTag 自主回充**、**6 轴机械臂 + 末端 D435i**、**有限指令集语音入口**
三条链路；自动速度源由一路变为两路，因此新增 `hr_motion_mux` 作为唯一仲裁点。

### 新增任务

| ID | 状态 | 任务与产物 | 前置依赖 | 验收或证据 | 阻塞原因/下一步 |
|---|---|---|---|---|---|
| F9-01 | 已实测 | `hr_motion_mux`：Nav2/Docking 自动速度源互斥仲裁，唯一发布 `/cmd_vel_auto` | F1-01 | 12 条单元测试 + 运行时 AC-1…AC-6 全通过，见 [hr_motion_mux验收](acceptance/hr_motion_mux验收.md) | 待与真实 Nav2 `controller_server` 联调 |
| F9-02 | 已实测 | 速度链改接线：Nav2 与 `hr_local_motion` 输出改为 `/cmd_vel_nav` | F9-01 | `ros2 topic info /cmd_vel_auto` publisher 计数为 1 | 同上 |
| F9-03 | 已实测 | `hr_task_manager` 发布 `DOCKING` 阶段与 `source_seq`，并 5 Hz 续发阶段授权 | F9-01 | 运行时验收发现并修复「事件式发布导致导航 1 秒后归零」 | 回充分支待电池数据接入 |
| F9-04 | 框架完成 | `hr_docking`：AprilTag 位姿门控、Tag—触点偏置换算、超时失效 | F9-01 | 11 条单元测试（DOCK-1…DOCK-7） | `apriltag_ros` 与 `opennav_docking` 均未安装；Tag 实物与外参未冻结 |
| F9-05 | 框架完成 | `hr_depth_obstacle`：深度点云质量门控与地面滤除 | Gemini 2 接入 | 11 条单元测试（DO-1…DO-7） | Gemini 2 实物未接入；量程/地面高度/有效率阈值待实测 |
| F9-06 | 框架完成 | `hr_arm_controller`：固定目标抓取状态机与安全互锁 | 机械臂硬件 | 12 条单元测试（ARM-1…ARM-6） | 机械臂、独立舵机控制器、D435i 三者均未到位；IK 未实现 |
| F9-07 | 框架完成 | `hr_voice_command`：白名单意图映射与拒绝策略 | 麦克风选型 | 12 条单元测试（V-1…V-6） | USB 麦克风/阵列与 ASR 引擎均未选型 |
| F9-08 | 待实施 | `hr_arm_perception` / `hr_arm_driver` / `hr_voice_capture` 实现 | F9-06、F9-07 | 骨架可构建，启动时主动拒绝（无标定/无端口/无 ASR） | 等硬件 |
| F9-09 | 阻塞 | Gemini 2 实物接入、标定与话题迁移 | Gemini 2 硬件 | §10.2「Gemini 2 感知链」全部验收项 | 相机未到位；当前 `hr_camera` 仍为 RTSP 调试取流 |
| F9-10 | 阻塞 | 充电桩实物、tag36h11 标定与 `docks.yaml` 冻结 | 充电桩、Tag 印制 | §10.3「AprilTag精对接 / 充电真实性」 | 硬件未到位；须按 §10.1 步骤 11–12 分四阶段推进，不得跳步 |
| F9-11 | 阻塞 | Collision Monitor 停止区多边形冻结 | 实测制动距离、总延迟 | 停止区覆盖轮廓 + 延迟行程 + 制动距离 + 安全余量 | 停止区当前仍为空，实车运动前必须先冻结 |

### 本轮变更后的构建与测试

- 全量构建：**22 个 ROS 2 包**通过（新增 7 个）。
- 新增纯逻辑单元测试 **58 条**，全部通过；仓库级测试保持通过。
- 运行时验收：`hr_motion_mux` 在真实 ROS graph 上验证了唯一发布者、未授权源不泄漏、
  切换归零窗口、源过期归零、授权过期归零和 `linear.y` 恒零六项。

### 本轮新增的历史件标记

- `hr_hmmd`：新技术方案中不存在毫米波链路，降级为历史调试件，不纳入正式部署图。
- `hr_vision`：历史视觉调试件。

两者均保留在仓库中以备回溯，但不再出现在正式节点拓扑与验收范围内。

---

## 2026-09-18（续）· 补齐缺失节点

上一轮把 `hr_arm_perception`、`hr_arm_driver`、`hr_voice_capture` 留作启动即拒绝的骨架，
`hr_arm_controller` 缺 IK 与 Action 服务端，Nav2/AMCL 只有配置没有节点组。本轮补齐。

| ID | 状态 | 任务与产物 | 前置依赖 | 验收或证据 | 阻塞原因/下一步 |
|---|---|---|---|---|---|
| F10-01 | 已实测 | `hr_arm_controller`：六轴逆运动学 + `/arm/execute` Action 服务端 | F9-06 | 23 条 IK 往返测试；6 项运行时验收，见 [机械臂链路验收](acceptance/机械臂链路验收.md) | 连杆长度为占位值，装车前必须实测替换 |
| F10-02 | 已实测 | `hr_arm_driver`：帧协议 + CRC16 + 重同步 + mock 舵机控制器 | F10-01 | 14 条协议测试；mock 后端在运行时验收中收敛到目标关节角 | 真实舵机控制器未选型，`transport=serial` 未验 |
| F10-03 | 框架完成 | `hr_arm_perception`：手眼标定校验 + 深度门控 + 抓取候选 | D435i | 28 条单元测试（标定过期/换机/换相机/透明物体/深度稀疏等） | 本机无 `vision_msgs`，节点未运行；D435i 未到位 |
| F10-04 | 已实测 | `hr_voice_capture`：唤醒门 + 可替换 ASR 适配器（text/vosk） | F9-07 | 12 条单元测试；6 项运行时验收，见 [语音链路验收](acceptance/语音链路验收.md) | 麦克风与 ASR 引擎未选型，vosk 适配器未运行 |
| F10-05 | 框架完成 | Nav2 节点组 + lifecycle_manager 接入 `navigation.launch.py` | F5-01 | 完整 `nav2_params.yaml` 通过 YAML 校验；22 包构建通过 | 速度/加速度/footprint 均为占位值；缺已验收地图，未实机跑通 |
| F10-06 | 框架完成 | AMCL + map_server 接入 `localization.launch.py` 的 navigation 模式 | F10-05 | 与 slam_toolbox 互斥（模式参数强制）；缺 `map:=` 时明确报错 | 缺已验收地图 |
| F10-07 | 已实测 | `scripts/install_deps.sh`：上游依赖一键安装 | 无 | 全部 apt 包名逐条核验存在；`--help` 正常 | `sllidar_ros2` 与 Orbbec 驱动不在 apt，脚本中已注明需源码构建 |
| F10-08 | 待实施 | `hr_task_manager` 接入 `ARM_*` 任务类型与 `VOICE` 来源 | F10-01、F9-07 | ExecuteTask → ExecuteArm 串联 | 本轮未做，是语音/机械臂端到端的最后一段 |

### 本轮修掉的两个缺陷

**1. `hr_arm_controller` 的 fail-open。** 原本从 `/robot_status` 读轮速判断底盘是否停稳，
写法是 `getattr(msg, 'wheel_vx', 0.0)`——但 `RobotStatus.msg` 里没有轮速字段，
于是「取不到数据」被兜底成「速度为零」，ARM-1 的四重确认里这一重恒为真。
改为订阅 `/wheel/odom_raw` 并加新鲜度检查，过期即**撤销**确认。
安全故障判据同时由单看 `safety_permit` 扩展为四条件任一成立。

**2. 逆运动学的符号配对写反。** `j3` 为正时肩角应取 `beta - alpha`，原写成 `beta + alpha`。
这个错误不抛异常、不越限，只返回一个**镜像了的姿态**——看起来合理，真机上就是抓空。
由「FK 生成位姿 → IK 还原」的往返测试抓出，修复后往返误差 1e-16 量级。

### 本轮构建与测试

- 全量构建：22 个 ROS 2 包通过。
- 纯逻辑单元测试：**136 条**全部通过（上一轮 59 条）。
- 运行时验收：速度仲裁 6 项 + 机械臂链路 6 项 + 语音链路 6 项 = **18 项**全过。

---

## 2026-09-18（三）· 上游依赖装齐后的补验

用户执行 `scripts/install_deps.sh` 后，`vision_msgs`、`apriltag_ros`、
`opennav_docking`、`robot_localization`、`slam_toolbox` 与 Nav2 全套节点均已就位。
此前跑不起来的节点第一次能接收**真实消息类型**，因而暴露了三个只有运行才发现的缺陷。

| ID | 状态 | 任务与产物 | 验收或证据 | 遗留 |
|---|---|---|---|---|
| F11-01 | 已实测 | 完整速度链拓扑与激光否决 | 7 项判据全过，见 [速度链路验收](acceptance/速度链路验收.md) | 真实 S3、真实 Nav2/Docking 输出未验 |
| F11-02 | 已实测 | `hr_arm_perception` 真实 `vision_msgs` 链路 | 8 项判据全过，反投影精确落在解析值上 | D435i 与真实手眼标定未到位 |
| F11-03 | 已实测 | `hr_dock_pose_adapter` 真实 `apriltag_msgs` + TF 链路 | 8 项判据全过 | Tag 印制件、外参、Docking Action 闭环未验 |
| F11-04 | 已实测 | `collision_monitor.yaml` 修正后可 configure/activate | 仓库配置实测到达 `active [3]` | 停止区尺寸仍为占位值 |

### 本轮修掉的三个缺陷

**1. `apriltag_msgs` 根本不含位姿。**
`AprilTagDetection` 只有像素几何（family / id / hamming / decision_margin /
centre / corners / homography），`apriltag_ros` 是通过 **TF** 发布 Tag 三维位姿的。
原实现假设了 `detection.pose.pose.pose`，一运行就抛异常。
改为：检测数组决定「这个 Tag 可不可信」，TF 决定「它在哪」。

**2. TF 缓存导致的 fail-open。**
改用 TF 之后立刻暴露：`tf2` Buffer 按缓存时长保留变换，用「最新可用」去查，
**广播停止之后仍返回上一帧**——相机已经看不见 Tag 了，节点还在发位姿。
修法是校验变换自身 `header.stamp` 的年龄。「查不到」和「查到的是旧的」都算失败。

**3. 停止区多边形的参数类型。**
`collision_monitor.yaml` 原写 `points: "[]"`（字符串），本机 nav2 版本把该参数
定型为 double 数组，configure 阶段直接报类型错误，节点停在 `unconfigured`。
改为扁平数组，并用一个明显保守的占位矩形代替空多边形——空多边形会让节点
根本不出现在 ROS graph 里，拓扑检查就分不清「还没接」和「接了但挂了」。

另外修正 `vision_msgs` 的嵌套字段访问（`bbox.center.position.x`、
`results[i].hypothesis.class_id`），与仓库既有的 `hr_target_tracker` 写法对齐。

### 本轮构建与测试

- 全量构建：22 个 ROS 2 包通过。
- 单元测试：**148 条**全部通过（`vision_msgs` 就位后 `hr_perception` 等也能跑了）。
- 运行时验收累计 **41 项**：速度仲裁 6 + 全链 7 + 机械臂 6 + 语音 6 + 末端视觉 8 + 回充位姿 8。

---

## 2026-09-18（四）· 平面仿真与真实 Nav2

此前导航段一直由「带真实耗时的 Action 服务端」顶替，真实 Nav2 从未串进链路，
因为跑 Nav2 需要地图，建图又需要一个能响应 `/cmd_vel` 并产生 `/scan` 的底盘。
这一轮补上这一环。

| ID | 状态 | 任务与产物 | 验收或证据 | 遗留 |
|---|---|---|---|---|
| F12-01 | 已实测 | `hr_planar_sim`：户型射线投射 + `/cmd_vel` 位姿积分 | 18 条几何单元测试；仿真器加载 33 段墙体并产出 720 线扫描 | 无碰撞检测（撞墙会穿过去），首版只为建图与导航验证 |
| F12-02 | 已实测 | 户型描述 `home_floorplan.yaml`：五个房间 + 门洞 + 家具 | 单元测试断言户型封闭、起始位姿不在墙里 | 与 `search_areas.yaml` 的房间命名一致 |
| F12-03 | 进行中 | `slam_toolbox` 建图并保存 `maps/home1` | 巡游脚本走遍五室 | — |
| F12-04 | 进行中 | 真实 Nav2 节点组在保存地图上跑通导航 | 速度链三话题各一个发布者 + 实际位移 | — |

### 为什么仿真器要模仿 STM32 的限制

`hr_planar_sim` 刻意复制了下位机的限速、加减速斜坡和 150 ms 命令超时。
一个接受真实底盘会拒绝的命令的仿真器，会把导航栈教坏——参数在仿真里调得很漂亮，
上车就发现底盘根本执行不了。诊断话题里它把自己标成 WARN 并写明
「simulated chassis and laser; no hardware attached」，不会被误认成实机数据。

里程计带高斯噪声而不是完美值：完美里程计会掩盖所有定位问题。

### 建图时踩到的时间戳竞态

第一版地图严重扭曲、墙外有大片扇形空白。表现看起来是「SLAM 参数没调好」，
放慢巡游、降低里程计噪声、加密关键帧都改善不了。

先验证仿真器本身：户型 33 段墙体，在 364 个内部位姿上各打 720 条射线，
**零条逃逸**——几何和射线投射都是对的。

真因在 slam_toolbox 日志里：`Message Filter dropping message ... queue is full`。
`publish_scan` 用 `now()` 打时间戳，而 `odom→base_link` 的 TF 由另一个定时器发布，
最新 TF 总是比扫描早最多 33 ms。于是每一帧扫描到达时都还**无法被变换**，
排队直到队列满被丢弃。

修法是让扫描复用上一次发布 TF 的时间戳，而不是取当前时间。
这类 bug 的特征是：症状出现在完全另一个模块（建图质量），
靠调那个模块的参数永远修不好。
